{% extends '_layout.twig' %}

{% block body %}
    {% set entryIds = craft.entries.ids() %}

    <hr>
    {{ sprig('_components/article1.twig') }}
    <hr>
    {{ sprig('_components/article2.twig') }}
{% endblock %}
